#include "OI.h"
#include "RobotMap.h"

OI::OI()
//:driveStick(new Joystick(JOYSTICK_PORT))
{
	// Process operator interface input here.
	/*Button* untilWallButton = new JoystickButton(driveStick,1);
	untilWallButton ->WhenPressed(new MoveUntilWall());*/
}
